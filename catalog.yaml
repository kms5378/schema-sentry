pipelines:
  - key: daily_revenue
    airflow_dag_id: daily_revenue
    owner: analytics
    criticality: critical
    inputs:
      - dataset: public.purchases
        columns: [purchased_at, amount]
    outputs:
      - dataset: mart.daily_revenue
        columns: [date, revenue]
